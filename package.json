{
  "name": "Whatsapp",
  "author": "Jan Kofroň",
  "description": "Electron-based whatsapp app",
  "version": "1.2.0",
  "main": "main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "start:show": "electron . --show",
    "pack:asar": "rm -rf dist/app dist/app.asar && mkdir -p dist/app && cp -r main.js preload.js package.json assets dist/app/ && asar pack dist/app dist/app.asar && rm -rf dist/app"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "@electron/asar": "^4.0.1",
    "electron": "^40.6.0"
  }
}
